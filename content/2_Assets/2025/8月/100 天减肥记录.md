这里是注释掉的代码 也是供留存了
<!--
```dataviewjs
// 获取所有笔记数据
const data = dv.pages('"日记"')
    .filter(p => p.date)
    .sort(p => p.date) // 按日期排序
    .map(p => ({
        date: p.date.toISODate(), // 格式化日期
        myWeight: p.my_weight,    // 我的体重
        wifeWeight: p.wife_weight // 妻子的体重
    }));

// 提取标签和数据
const labels = data.map(p => p.date); // 日期作为横轴标签
const myWeights = data.map(p => p.myWeight); // 我的体重数据
const wifeWeights = data.map(p => p.wifeWeight); // 妻子的体重数据

// 打印调试信息
console.log("我的体重数据：", myWeights);
console.log("佳佳的体重数据：", wifeWeights);
// 生成 Obsidian Charts 的代码
const chartCode = `
\`\`\`chart
type: line
labels: [${labels.map(d => `"${d}"`).join(", ")}]
series:
  - title: 我的体重
    data: [${myWeights.join(", ")}]
  - title: 佳佳的体重
    data: [${wifeWeights.join(", ")}]
\`\`\`
`;

// 渲染图表
dv.span("### 体重趋势");
dv.span(chartCode);
```
-->
这个是当时的快照
![[2_Assets/2025/8月/image.png]]
#已归档 